name: scheduler
description: A library for managing assignment scheduling.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

dependencies:
  collection: ^1.19.1
  meta: ^1.18.2

dev_dependencies:
  checks: ^0.3.1
  lints: ^6.1.0
  test: ^1.31.1
